---
"@scaleway/changesets-check": minor
---

Add `@scaleway/changesets-check` package: a CLI that validates changesets are present and parseable by assembling a release plan, exiting non-zero on invalid or missing changesets. Useful as a CI gate.
