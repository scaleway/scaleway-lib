{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@babel/core": "^7.12.3",
    "@babel/plugin-transform-runtime": "^7.12.1",
    "@babel/preset-env": "^7.12.1",
    "@commitlint/cli": "^11.0.0",
    "@commitlint/config-conventional": "^11.0.0",
    "@rollup/plugin-babel": "^5.2.1",
    "@rollup/plugin-commonjs": "^16.0.0",
    "@rollup/plugin-node-resolve": "^10.0.0",
    "builtin-modules": "^3.1.0",
    "cross-env": "^7.0.2",
    "eslint": "^7.14.0",
    "eslint-formatter-gitlab": "^2.0.0",
    "husky": "^4.3.0",
    "jest": "^26.6.3",
    "jest-junit": "^12.0.0",
    "lerna": "3.22.1",
    "lint-staged": "^10.5.1",
    "prettier": "^2.2.0",
    "read-pkg": "^5.2.0",
    "rollup": "^2.33.3",
    "rollup-plugin-analyzer": "^3.3.0",
    "rollup-plugin-preserve-shebangs": "^0.2.0"
  },
  "scripts": {
    "lint": "eslint --cache .",
    "lint:fix": "yarn run lint --fix",
    "build": "lerna exec --ignore @scaleway/eslint-* --ignore @scaleway/countries -- rollup -c ../../rollup.config.js",
    "build:profile": "cross-env PROFILE=true yarn run build",
    "commit": "npx git-cz -a",
    "test": "jest",
    "test:watch": "yarn run test --watch",
    "test:coverage": "yarn run test --coverage"
  },
  "commitlint": {
    "extends": [
      "@commitlint/config-conventional"
    ]
  },
  "husky": {
    "hooks": {
      "commit-msg": "commitlint -E HUSKY_GIT_PARAMS",
      "pre-commit": "lint-staged",
      "pre-push": "lint-staged"
    }
  },
  "lint-staged": {
    "*.js": [
      "prettier --write",
      "eslint --fix"
    ]
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  },
  "jest": {
    "collectCoverageFrom": [
      "packages/*/src/*.{js,jsx}",
      "!packages/didiff-lib/capture/screenshot.js"
    ],
    "coverageReporters": [
      "text",
      "cobertura"
    ],
    "reporters": [
      "default",
      [
        "jest-junit",
        {
          "outputDirectory": ".reports",
          "outputName": "tests.xml"
        }
      ]
    ]
  }
}
